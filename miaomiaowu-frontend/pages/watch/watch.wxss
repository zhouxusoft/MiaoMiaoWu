/* pages/watch/watch.wxss */
page {
	display: flex;
	flex-direction: column;
}

.scrollarea {
	flex: 1;
	overflow: hidden;
}

.custom-tabs {
	margin-bottom: 32rpx;
}

.custom-panel {
	height: 1000px;
	color: var(--td-text-color-primary);
}